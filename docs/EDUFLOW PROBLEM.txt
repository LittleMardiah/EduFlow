================================================================================
HANDOFF DOCUMENT - EDUFLOW (FASE 2 DAY 6-7)
================================================================================
Tujuan: Memberikan konteks penuh kepada AI lain agar bisa melanjutkan debugging 
        dan menyelesaikan Task RBAC, RLS & Permission Enforcement.

================================================================================
1. IDENTITAS PROYEK
================================================================================
Nama Projek     : EduFlow (All-in-One EdTech Platform)
Fase Saat Ini   : FASE 2 (Quiz & Question Management) - Day 6-7
Teknologi       : Node.js (18 LTS), TypeScript, Express, Prisma (5.22.0)
Database        : PostgreSQL (Neon.tech)
Package Manager : pnpm
Status Server   : ❌ SERVER TIDAK BISA JALAN (TypeScript Error)

================================================================================
2. TASK YANG DIBERIKAN (DARI ROADMAP_FASE_2.md)
================================================================================
Day 6-7: RBAC, RLS & Permission Enforcement

1. Create Ownership Middleware (`src/middleware/ownership.middleware.ts`)
   - Sudah selesai dan isinya benar (support quiz, question, option).
   - Sudah terintegrasi di routes.

2. Add RLS Policies for Quizzes
   - Migration SQL sudah dibuat dan dijalankan di Neon.tech.
   - Fungsi `current_user_id()` dan `current_user_role()` sudah dibuat (return TEXT).
   - `auth.middleware.ts` sudah di-update untuk SET `app.current_user_id` dan `app.current_user_role`.

3. Test RBAC Enforcement
   - Target: Student POST /quizzes → 403, Instructor B cannot edit A's quiz, etc.
   - **STATUS: BELUM BERHASIL** karena server tidak bisa jalan.

================================================================================
3. ROOT CAUSE UTAMA (KENAPA SERVER DOWN & TEST GAGAL)
================================================================================
❗ ERROR: TypeScript compilation gagal karena mismatch antara Repository dan Service.

Ada 2 source file utama yang bermasalah:

A. `src/services/quiz.service.ts` mengimpor fungsi-fungsi yang TIDAK ADA di 
   `src/repositories/quiz.repository.ts`:
   - findQuizById
   - findQuizzes
   - countQuizzes
   - softDeleteQuiz (sebagai softDeleteQuizRepo)
   - restoreQuiz (sebagai restoreQuizRepo)
   - getQuizVersions
   - createQuizVersion

B. `src/services/question.service.ts` mengimpor fungsi yang TIDAK ADA di 
   `src/repositories/quiz.repository.ts`:
   - incrementTotalQuestions
   - decrementTotalQuestions

C. `src/repositories/quiz.repository.ts` yang sekarang hanya memiliki:
   - createQuiz
   - getQuizById
   - updateQuiz
   - deleteQuiz
   - listQuizzes
   - (belum ada yang lain)

================================================================================
4. FILE-FILE YANG HARUS DICEK / DIPERBAIKI OLEH AI BERIKUTNYA
================================================================================
Silakan cek dan perbaiki file-file ini (prioritas tinggi):

1. src/repositories/quiz.repository.ts
   -> Tambahkan export untuk fungsi yang diminta oleh service, atau sesuaikan 
      import di service dengan fungsi yang sudah ada.
   -> Idealnya implementasikan:
      - findQuizById(id) -> getQuizById(id) [rename atau alias]
      - findQuizzes(filters) -> listQuizzes(filters)
      - countQuizzes(filters)
      - softDeleteQuiz(id) -> panggil update dengan deleted_at
      - restoreQuiz(id) -> update set deleted_at = null
      - getQuizVersions(quizId)
      - createQuizVersion(data)
      - incrementTotalQuestions(quizId)
      - decrementTotalQuestions(quizId)

2. src/services/quiz.service.ts
   -> Sesuaikan import atau panggil repository yang sudah diperbaiki.

3. src/services/question.service.ts
   -> Perbaiki import untuk incrementTotalQuestions & decrementTotalQuestions.

4. src/index.ts & src/app.ts (Isu kedua)
   -> Saat ini ada upaya memisahkan `app` (untuk test) dan `index` (untuk server).
   -> AI berikutnya bisa memutuskan: apakah akan pertahankan struktur app.ts 
      (ekspor app untuk supertest) atau rollback ke index.ts biasa.
   -> Saat ini import di `tests/integration/rbac.test.ts` menggunakan `../../src/app`.
   -> Pastikan path tersebut benar.

5. tests/integration/rbac.test.ts
   -> Sudah dibuat dan sudah mencoba mengirim `organization_id`.
   -> Tapi gagal karena server error (masalah nomor 1 di atas).
   -> Setelah repository service fix, test ini harusnya bisa jalan.

6. src/schemas/quiz.schemas.ts
   -> Periksa apakah `organization_id` diizinkan di schema validasi (Zod).
   -> Jika tidak, tambahkan `organization_id: z.string()` agar payload tidak di-strip.

7. src/controllers/quiz.controller.ts
   -> Pastikan `organization_id` dari request diteruskan ke service/repository.
   -> Jika service tidak menerima `organization_id`, tambahkan parameter.

================================================================================
5. LINGKUNGAN & PERINTAH
================================================================================
- Jalankan server: `pnpm dev`
- Jalankan test: `pnpm test:rbac`
- Cek TypeScript: `npx tsc --noEmit`
- Database: Neon.tech (connection string ada di .env)
- Pastikan port 3000 tidak digunakan sebelum test (matikan server jika running).

================================================================================
6. APA YANG HARUS DILAKUKAN AI BERIKUTNYA?
================================================================================
1. Fokus utama: **Perbaiki error TypeScript** dengan menyelaraskan Repository & Service.
   - Opsi A: Tambahkan semua fungsi yang hilang di repository.
   - Opsi B: Ubah semua import di service agar sesuai dengan repository yang ada.
   - **Rekomendasi:** Opsi A (tambahkan fungsi) karena lebih bersih untuk jangka panjang.

2. Setelah TypeScript bersih, jalankan server (`pnpm dev`) untuk memastikan tidak ada runtime error.

3. Jalankan test (`pnpm test:rbac`) dan lihat apakah ada error terkait foreign key 
   (`organization_id`). Jika masih error, cek poin 6 dan 7 di atas (Schema & Controller).

4. Jika test berhasil (5 skenario passing), tugas Day 6-7 selesai.

================================================================================
7. CATATAN TAMBAHAN
================================================================================
- Fungsi RLS di database sudah siap, tinggal di-test via aplikasi.
- Ownership middleware sudah siap.
- Saya (AI sebelumnya) sudah melakukan banyak debugging dan sampai di titik stuck 
  karena masalah struktural ini. AI berikutnya diharapkan bisa menyelesaikan 
  mismatch struktural tanpa harus mengubah arsitektur besar.
- Gunakan `pnpm` untuk semua perintah.

================================================================================
SELAMAT BEKERJA! 🚀
================================================================================