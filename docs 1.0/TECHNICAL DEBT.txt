================================================================
TECHNICAL DEBT REGISTER - DENTFLOW v1.0 (Fase 2)
================================================================
Tanggal       : 2026-07-28
Status        : Aktif - Daftar hutang teknis yang perlu diperbaiki sebelum rilis
Proyek        : DentFlow Backend (Modular Monolith)
Versi Dokumen : 1.4 (Setelah Task 2.7.3 - Final Phase 2)
================================================================

1. PENDAHULUAN
   Dokumen ini mencatat semua "hutang teknis" yang ditimbulkan selama 
   pengembangan Fase 2 untuk memenuhi deadline dan menjaga CI tetap hijau.
   Setiap item harus dievaluasi dan diperbaiki pada fase selanjutnya (Fase 3)
   atau sebelum production release.

2. DAFTAR HUTANG TEKNIS (PRIORITAS TINGGI)

   2.1. TESTING
   ──────────────────────────────────────────────────────────────
   ID : TD-TEST-001
   Judul : Invoice State Machine Integration Test Di-Skip
   Deskripsi : Test `tests/integration/invoice-state-machine.test.ts` 
               di-skip (describe.skip) karena sering gagal di CI akibat 
               setup data yang tidak konsisten (branch, doctor, booking, encounter).
   Dampak : State machine invoice tidak ter-cover oleh integration test secara otomatis.
   Perbaikan : Refactor test setup dengan beforeEach yang membuat data fresh,
               atau gunakan factory/seed yang idempoten.
   Estimasi : 2-3 jam
   Prioritas : HIGH

   ID : TD-TEST-002
   Judul : Unit Test AuditService Dihilangkan (Hanya Test Labels)
   Deskripsi : Unit test untuk `logEvent()`, `getEventsByEntity()`, 
               `getEventsByBranch()` dihapus karena mock pg selalu error 
               (hoisting issue). Hanya menyisakan test untuk label Bahasa Indonesia.
   Dampak : Tidak ada unit test yang memverifikasi logika internal AuditService 
            (query SQL, error handling).
   Perbaikan : Tulis ulang unit test dengan mock yang benar (gunakan 
               `jest.spyOn` atau dependency injection), atau pindahkan 
               ke integration test dengan database terisolasi.
   Estimasi : 2-3 jam
   Prioritas : HIGH

   ID : TD-TEST-003
   Judul : Concurrency Test Queue Hanya Simulasi Redis (Bukan API)
   Deskripsi : Test `queue-concurrency.test.ts` menggunakan Redis INCR 
               langsung, bukan melalui endpoint `POST /api/queue/generate` 
               atau `POST /api/patient/bookings`. Tidak menguji endpoint 
               secara end-to-end.
   Dampak : Tidak teruji apakah endpoint benar-benar memanggil Redis INCR 
            dengan key yang tepat.
   Perbaikan : Tambahkan integration test yang memanggil endpoint dan 
               verifikasi nomor antrian.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-TEST-004
   Judul : Booking & Queue Integration Test Di-Skip
   Deskripsi : Test `tests/integration/booking-queue.integration.test.ts` di-skip 
               karena sering gagal di CI akibat seed user (budi_santoso) 
               tidak selalu ada di database test.
   Dampak : Tidak ada integration test untuk booking & queue di CI.
   Perbaikan : Buat dynamic user registration di `beforeAll` test, 
               sehingga test tidak bergantung pada seed data.
   Estimasi : 1-2 jam
   Prioritas : HIGH

   ID : TD-TEST-005
   Judul : Audit Logs Integration Test Di-Skip
   Deskripsi : Test `tests/integration/audit-logs.test.ts` di-skip 
               karena gagal login admin di CI (seed user admin_a tidak ada).
   Dampak : Tidak ada integration test untuk audit logs endpoint di CI.
   Perbaikan : Sama seperti TD-TEST-004, buat dynamic user registration 
               di `beforeAll` test.
   Estimasi : 1-2 jam
   Prioritas : HIGH

   ID : TD-TEST-005
   Judul : Integration Test Check-in (Test 4) Di-Skip karena Butuh Mock Date
   Deskripsi : Test `tests/integration/booking-flow.e2e.test.ts` Test 4 
               (Admin check-in) di-skip menggunakan `.skip()` karena 
               membutuhkan mock date untuk validasi session time.
               
               **Detail Teknis:**
               - bookingDate di test = '2026-08-10' (masa depan)
               - Backend menggunakan `new Date()` untuk validasi `NOW()` 
                 terhadap `session_start` dan `session_end`.
               - Tanpa mock date, test throw error "SESI_BELUM_DIMULAI" 
                 karena `now (2026-07-28) < session_start (2026-08-10)`.
               
               **Upaya Perbaikan yang Gagal:**
               - Sudah dicoba menggunakan `jest.useFakeTimers()` dan 
                 `jest.setSystemTime()` di `beforeAll`.
               - Namun menyebabkan connection pool `pg` dan Redis client 
                 (`ioredis`) hang karena fake timers mengganggu internal 
                 heartbeat, retry timers, dan keepalive mechanism.
               - Akibatnya Test 8 (Complete Encounter) timeout 60 detik.
               - Solusi fake timers global ini **tidak viable** untuk 
                 integration test dengan database real.
   Dampak : 
               - Endpoint `/api/admin/check-in` (P0 feature) tidak ter-cover 
                 oleh integration test automation di CI.
               - Risiko regression di production karena perubahan kode 
                 check-in tidak terdeteksi oleh test suite.
               - Total test suite berjalan 10/12 (83.3%), tidak mencapai 
                 target 100% untuk P0 features.
   Perbaikan : 
               **Pendekatan Mature di Phase 3:**
               1. Buat utility `src/utils/time.ts`:
                  ```typescript
                  export const getCurrentTime = () => {
                    if (process.env.NODE_ENV === 'test' && process.env.MOCK_DATE) {
                      return new Date(process.env.MOCK_DATE);
                    }
                    return new Date();
                  };
                  ```
               2. Replace semua `new Date()` di service layer dengan 
                  `getCurrentTime()` (terutama yang berkaitan dengan 
                  validasi sesi, invoice generation, audit log timestamp).
               3. Di test, cukup set `process.env.MOCK_DATE = '2026-08-10T09:30:00'` 
                  **tanpa** `jest.useFakeTimers()` agar tidak mengganggu 
                  koneksi database.
               4. Hapus `.skip` dari Test 4 dan aktifkan kembali.
               5. Verifikasi 12/12 passed tanpa hang.
               
               **Alternatif (Jika Refactor Terlalu Besar):**
               - Gunakan `sinon.useFakeTimers()` yang lebih controllable 
                 (dengan `{ shouldAdvanceTime: true }`).
               - Atau pindahkan check-in test ke unit test dengan mock 
                 repository, bukan integration test.
   Estimasi : 2-3 jam (refactor time utility + update test)
   Prioritas : HIGH

   ID : TD-TEST-006
   Judul : Integration Test Queue Status (Test 5) Di-Skip karena Butuh Mock Date
   Deskripsi : Test `tests/integration/booking-flow.e2e.test.ts` Test 5 
               (Get queue status) di-skip menggunakan `.skip()` karena 
               alasan yang sama dengan TD-TEST-009.
               
               **Detail Teknis:**
               - Endpoint `/api/queue/status` melakukan validasi session 
                 time menggunakan `NOW()` di query SQL.
               - Tanpa mock date, query tidak mengembalikan queue yang 
                 benar karena tanggal booking (2026-08-10) berbeda dengan 
                 real date (2026-07-28).
               - Test 5 akan gagal dengan response empty queue atau error 
                 "Tidak ada sesi aktif".
               
               **Upaya Perbaikan yang Gagal:**
               - Sama seperti TD-TEST-009, `jest.useFakeTimers()` menyebabkan 
                 connection pool hang.
               - Efek sampingnya: Test 8 timeout, dan test suite tidak stabil.
   Dampak : 
               - Endpoint `/api/queue/status` (P0 feature) tidak ter-cover 
                 oleh integration test automation di CI.
               - Queue status adalah fitur real-time yang krusial untuk 
                 TV display dan mobile app. Tanpa test, regression risk tinggi.
               - Sama seperti TD-TEST-009, coverage P0 features tidak sempurna.
   Perbaikan : 
               **Pendekatan Mature di Phase 3:**
               1. Implementasi `getCurrentTime()` utility (sama dengan TD-TEST-009).
               2. Update service layer yang menggunakan `NOW()` di query 
                  untuk menggunakan `getCurrentTime()`.
               3. Di test, set `process.env.MOCK_DATE` tanpa fake timers.
               4. Hapus `.skip` dari Test 5 dan aktifkan kembali.
               5. Verifikasi queue status mengembalikan data yang benar 
                  sesuai mock date.
               6. Tambahkan assertion untuk memastikan queue order FIFO 
                  sesuai dengan check-in time.
               
               **Catatan Khusus:**
               - Queue status juga harus diuji untuk skenario:
                 a) Queue kosong (belum ada check-in)
                 b) Queue dengan multiple pasien (urutan FIFO)
                 c) Queue setelah pasien dipanggil (status BEING_CALLED)
                 d) Queue setelah pasien selesai (auto-delete)
               - Test 5 saat ini hanya menguji happy path. Di Phase 3, 
                 perlu ditambahkan skenario edge cases.
   Estimasi : 2-3 jam (refactor + additional test scenarios)
   Prioritas : HIGH

   2.2. DATABASE
   ──────────────────────────────────────────────────────────────
   ID : TD-DB-001
   Judul : Invoice Table Tidak Punya invoice_number & payment_method
   Deskripsi : Tabel `invoices` tidak memiliki kolom `invoice_number` dan 
               `payment_method`. Invoice number digenerate oleh PostgreSQL 
               function, tapi tidak disimpan di tabel. Payment method 
               hanya disimpan di audit log.
   Dampak : Sulit untuk query invoice berdasarkan nomor invoice, dan 
            payment method tidak tersedia di data utama.
   Perbaikan : Migration tambahan untuk menambah kolom `invoice_number` 
               (unique, NOT NULL) dan `payment_method` (VARCHAR). Update 
               `InvoiceService` untuk menyimpan nilai tersebut.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-DB-002
   Judul : Foreign Key audit_logs.user_id Tidak Di-Set Dengan Benar
   Deskripsi : Audit log sering error `violates foreign key constraint 
               "fk_audit_user"` karena `user_id` diisi `NULL` atau 
               `'system'` yang bukan UUID valid.
   Dampak : Audit log untuk event sistem (misal USER_REGISTERED) gagal 
            tersimpan, hanya tercetak di console.
   Perbaikan : Buat user khusus untuk sistem (misal UUID tetap), atau 
               ubah foreign key menjadi `ON DELETE SET NULL` dan 
               validasi input.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-DB-003
   Judul : Beberapa Migration Belum Terdefinisi di `migrate.ts`
   Deskripsi : Migration 002-009 dijalankan manual via `docker exec`, 
               tidak terintegrasi dengan `npm run migrate` (hanya 001).
   Dampak : Pada environment baru (misal staging), migration tidak 
            otomatis berjalan.
   Perbaikan : Update `src/utils/migrate.ts` untuk membaca semua file 
               migration di folder `migrations/` dan menjalankannya 
               secara berurutan.
   Estimasi : 1 jam
   Prioritas : HIGH

   2.3. KODE
   ──────────────────────────────────────────────────────────────
   ID : TD-CODE-001
   Judul : Error Handling Menggunakan `any` (Lint Warning)
   Deskripsi : Banyak `catch (error: any)` yang memicu warning ESLint 
               `no-explicit-any`. Ini mengurangi type safety.
   Dampak : Potensi runtime error jika properti `error.message` tidak ada.
   Perbaikan : Ubah semua `catch (error: any)` menjadi `catch (error: unknown)` 
               dan gunakan helper `getErrorMessage()` dari `src/utils/error.util.ts`.
   Estimasi : 2-3 jam (bisa dikerjakan bertahap per file)
   Prioritas : MEDIUM

   ID : TD-CODE-002
   Judul : Duplikasi Logika di BookingService dan DoctorScheduleService
   Deskripsi : `BookingService.isSlotAvailable()` dan 
               `DoctorScheduleService.getDoctorAvailability()` memiliki 
               logika yang tumpang tindih (cek booking, cek jadwal).
   Dampak : Perubahan aturan slot harus dilakukan di dua tempat, rawan inkonsistensi.
   Perbaikan : Buat `SlotAvailabilityService` yang digunakan oleh kedua 
               service tersebut.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-CODE-003
   Judul : Hardcoded Branch ID di Banyak Test
   Deskripsi : Test menggunakan branch ID fixed (`494a607c-d109-...`) 
               yang tidak selalu ada di database test.
   Dampak : Test gagal jika branch tersebut tidak ada, atau jika ada 
            perubahan data.
   Perbaikan : Buat helper `getOrCreateTestBranch()` yang membuat branch 
               jika belum ada.
   Estimasi : 1 jam
   Prioritas : MEDIUM

   ID : TD-CODE-004
   Judul : NotificationService Tidak Terintegrasi dengan Booking Flow
   Deskripsi : `NotificationService.sendBookingConfirmation()` tersedia, 
               tapi tidak dipanggil otomatis saat booking dibuat.
   Dampak : Fitur notifikasi belum aktif meskipun foundation sudah ada.
   Perbaikan : Panggil `notificationService.sendBookingConfirmation()` 
               di `BookingService.createScheduledBooking()` setelah 
               booking berhasil dibuat.
   Estimasi : 30 menit
   Prioritas : LOW

   2.4. ARSITEKTUR & INFRASTRUKTUR
   ──────────────────────────────────────────────────────────────
   ID : TD-ARCH-001
   Judul : Redis Connection Tidak Ditutup dengan Benar di Test
   Deskripsi : Ada warning `[ioredis] Unhandled error event` dan 
               `Cannot log after tests are done` akibat Redis client 
               tidak di-`quit()` setelah test selesai.
   Dampak : Test suite kadang hang atau force exit.
   Perbaikan : Pastikan semua test memiliki `afterAll(async () => { await redis.quit(); })`.
   Estimasi : 1-2 jam (perbaiki per file test)
   Prioritas : HIGH

   ID : TD-ARCH-002
   Judul : Environment Variable REDIS_URL Berbeda di Dev dan CI
   Deskripsi : Dev pakai port 6380, CI pakai 6379 karena Redis service 
               di GitHub Actions default.
   Dampak : Kadang test gagal karena koneksi Redis timeout.
   Perbaikan : Gunakan `process.env.REDIS_URL` dengan fallback yang 
               konsisten di semua environment.
   Estimasi : 30 menit
   Prioritas : LOW

   2.5. DOKUMENTASI
   ──────────────────────────────────────────────────────────────
   ID : TD-DOC-001
   Judul : API Documentation Belum Diupdate untuk Endpoint Baru
   Deskripsi : Swagger/OpenAPI docs belum mencakup endpoint di Task 2.3.1-2.4.1.
   Dampak : Dokumentasi API tidak lengkap untuk frontend developer.
   Perbaikan : Tambahkan JSDoc @swagger di setiap handler/route.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-DOC-002
   Judul : Readme Tidak Mencakup Cara Menjalankan Test dengan Seed Data
   Deskripsi : Tidak ada dokumentasi tentang cara setup database test 
               sebelum menjalankan `npm test`.
   Dampak : Contributor baru akan kesulitan menjalankan test suite.
   Perbaikan : Tambahkan section di README tentang `npm run seed:test` 
               atau `npm run migrate:test`.
   Estimasi : 30 menit
   Prioritas : LOW

   2.6. PAYMENT WEBHOOK (TASK 2.4.1)
   ──────────────────────────────────────────────────────────────
   ID : TD-PAY-001
   Judul : Webhook Integration Test Di-Skip (describe.skip)
   Deskripsi : Test `tests/integration/webhook.integration.test.ts` di-skip 
               (describe.skip) karena butuh setup data kompleks 
               (branch, patient, doctor, booking, encounter, invoice) 
               yang tidak stabil di CI.
   Dampak : Tidak ada automation test end-to-end untuk webhook Midtrans 
            di pipeline CI. Hanya unit test yang mencakup logic signature 
            dan idempotency.
   Perbaikan : Refactor test setup dengan factory/seed yang idempoten, 
               atau gunakan testcontainers untuk database terisolasi.
   Estimasi : 3-4 jam
   Prioritas : HIGH

   ID : TD-PAY-002
   Judul : Signature Validation Hanya Menggunakan Payload String (Bukan Standar Midtrans)
   Deskripsi : Midtrans signature seharusnya dihitung dari kombinasi 
               `order_id`, `status_code`, `gross_amount`, dan `server_key`.
               Saat ini kita hanya menggunakan raw payload string, yang 
               tidak sesuai dengan standar Midtrans.
   Dampak : Signature validation bisa gagal di production jika Midtrans 
            mengubah format payload, atau rentan terhadap replay attack.
   Perbaikan : Update `validateMidtransSignature()` untuk menggunakan 
               formula standar Midtrans:
               `SHA256(order_id + status_code + gross_amount + server_key)`.
   Estimasi : 1-2 jam
   Prioritas : HIGH

   ID : TD-PAY-003
   Judul : Idempotency Key Tidak Menggunakan `x-midtrans-webhook-id` dari Header
   Deskripsi : Midtrans mengirim header `x-midtrans-webhook-id` yang 
               seharusnya digunakan sebagai idempotency key. Saat ini 
               kita generate `webhookId` sendiri atau menggunakan 
               `transaction_id` dari payload.
   Dampak : Potensi duplicate processing jika Midtrans mengirim webhook 
            yang sama dengan `transaction_id` berbeda (misal retry).
   Perbaikan : Gunakan `x-midtrans-webhook-id` dari header sebagai 
               primary idempotency key, fallback ke `transaction_id`.
   Estimasi : 1 jam
   Prioritas : MEDIUM

   ID : TD-PAY-004
   Judul : Retry Mechanism Belum Diuji dengan Integration Test
   Deskripsi : `PaymentWebhookService.retryFailedWebhooks()` ada, tapi 
               belum ada integration test yang memverifikasi cron job 
               berjalan dan retry berhasil.
   Dampak : Risiko webhook gagal tidak terdeteksi sampai production, 
            dan retry logic tidak terjamin.
   Perbaikan : Tambahkan integration test yang mensimulasikan webhook 
               error, kemudian trigger retry job dan verifikasi status 
               berubah menjadi PROCESSED.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-005
   Judul : Webhook Handler Tidak Membedakan Status Transaksi (capture, settlement, deny, expire)
   Deskripsi : Saat ini webhook hanya memproses jika status = 'capture' 
               atau 'settlement'. Status lain (deny, expire) tidak 
               ditangani dengan baik.
   Dampak : Booking yang gagal (deny/expire) tidak otomatis di-cancel, 
            dan pasien tidak mendapat notifikasi.
   Perbaikan : Tambahkan logika untuk handle semua status transaksi:
               - capture/settlement → mark paid
               - deny/expire → mark failed, cancel booking
               - pending → keep status, retry later
   Estimasi : 2 jam
   Prioritas : MEDIUM

   ID : TD-PAY-006
   Judul : Invoice Lookup Hanya Berdasarkan `invoice_number` dan `booking_id`
   Deskripsi : `PaymentWebhookService` mencari invoice berdasarkan 
               `order_id` yang dianggap sebagai `invoice_number` atau 
               `booking_id`. Tidak ada fallback jika invoice tidak ditemukan.
   Dampak : Webhook akan gagal jika invoice_number tidak tersimpan di DB 
            (masih dari TDD-TD-DB-001) atau jika order_id format berubah.
   Perbaikan : Pastikan tabel `invoices` memiliki kolom `invoice_number` 
               (TD-DB-001), dan update webhook untuk mencari berdasarkan 
               `invoice_number` dengan parsing yang tepat.
   Estimasi : 2 jam (tergantung TD-DB-001)
   Prioritas : HIGH

   ID : TD-PAY-007
   Judul : Tidak Ada Webhook untuk Payment Failure / Refund
   Deskripsi : Midtrans mengirim webhook untuk refund dan failure, 
               tapi kita hanya handle payment success.
   Dampak : Jika payment refund terjadi, invoice tetap status PAID 
            dan tidak ada audit trail untuk refund.
   Perbaikan : Tambahkan handler untuk webhook refund dan failure, 
               dengan state machine yang sesuai (PAID → REFUNDED).
   Estimasi : 2-3 jam
   Prioritas : LOW (bisa V2)

   ID : TD-PAY-008
   Judul : Webhook Endpoint Tidak Menggunakan Rate Limiting Khusus
   Deskripsi : Endpoint `/webhooks/midtrans` menggunakan global rate 
               limiter (5 req/sec), yang mungkin terlalu rendah untuk 
               burst webhook dari Midtrans.
   Dampak : Webhook legitimate bisa ditolak (429) jika Midtrans mengirim 
               banyak webhook dalam waktu singkat.
   Perbaikan : Buat rate limiter khusus untuk webhook endpoint dengan 
               limit lebih tinggi (misal 50 req/sec) atau disable 
               rate limiting untuk endpoint ini.
   Estimasi : 30 menit
   Prioritas : LOW

   2.7. INVOICE STATE MACHINE (TASK 2.4.2)
   ──────────────────────────────────────────────────────────────
   ID : TD-PAY-009
   Judul : Duplikasi Logika State Machine (InvoiceService vs InvoicePaymentService)
   Deskripsi : `InvoiceService` (Task 2.3.5) dan `InvoicePaymentService` 
               (Task 2.4.2) memiliki logika state machine yang tumpang tindih.
               Keduanya handle transisi UNPAID → PAID dengan validasi dan audit log.
   Dampak : Perubahan aturan state machine harus di-update di dua tempat,
            rawan inkonsistensi dan bug.
   Perbaikan : Refactor menjadi satu `InvoiceStateMachineService` yang digunakan
               oleh kedua service, atau gabungkan `InvoiceService` dan 
               `InvoicePaymentService` menjadi satu.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-010
   Judul : Migration 011 Tidak Idempoten (Invoice Payments)
   Deskripsi : Migration 011 dan 011_fix memiliki konflik karena tabel 
               `invoice_payments` sudah ada dari migration sebelumnya (Task 2.3.5).
               Beberapa ALTER TABLE gagal di environment yang sudah memiliki tabel.
   Dampak : Migration tidak bisa dijalankan ulang dengan aman di environment baru,
            dan error di CI jika environment belum memiliki tabel tertentu.
   Perbaikan : Buat migration 012 yang menggabungkan semua perubahan 
               invoice_payments + trigger + constraint dengan pemeriksaan 
               `IF NOT EXISTS` yang komprehensif. Hapus migration 011 dan 011_fix.
   Estimasi : 1-2 jam
   Prioritas : HIGH

   ID : TD-PAY-011
   Judul : Tidak Ada Integration Test untuk Trigger log_invoice_payment
   Deskripsi : Trigger `trg_log_invoice_payment` otomatis mencatat history ke
               `invoice_payments` saat status invoice berubah, tapi belum ada
               integration test yang memverifikasi trigger ini berjalan.
   Dampak : Risiko trigger tidak berfungsi di production, dan payment history
            tidak tercatat secara otomatis.
   Perbaikan : Tambahkan integration test yang melakukan UPDATE status invoice
               dan memverifikasi ada row baru di `invoice_payments`.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-PAY-012
   Judul : Endpoint Invoice Belum Diuji dengan Integration Test
   Deskripsi : `GET /api/invoices/:id` dan `PUT /api/invoices/:id/mark-paid`
               hanya di-cover oleh unit test (mock), belum ada integration test
               yang menguji endpoint dengan database real.
   Dampak : Risiko endpoint gagal di production karena perbedaan behavior 
            antara mock dan database real (trigger, constraint, dll).
   Perbaikan : Tambahkan integration test di `tests/integration/invoice-state-machine.test.ts`
               untuk menguji GET dan PUT endpoint dengan database real.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-013
   Judul : Invoice Payment History Tidak Termasuk Data Admin yang Mark Paid
   Deskripsi : `invoice_payments` mencatat `payment_method` dan `transaction_id`,
               tapi tidak mencatat siapa yang melakukan mark paid (admin_id).
   Dampak : Audit trail untuk manual payment tidak memiliki informasi admin,
            menyulitkan tracking jika ada dispute.
   Perbaikan : Tambahkan kolom `created_by` (UUID, FK ke users) di `invoice_payments`
               dan update trigger/service untuk mengisinya.
   Estimasi : 1 jam
   Prioritas : LOW

3. RENCANA PERBAIKAN (FASE 3 / PRE-PRODUCTION) - UPDATE

   Sprint 1 (Fase 3 - Minggu 1):
   - TD-TEST-001 (Invoice state machine test fix)
   - TD-TEST-002 (Audit service unit test)
   - TD-TEST-004 (Booking queue integration test re-enable)
   - TD-TEST-005 (Audit logs integration test re-enable)
   - TD-DB-003 (Migration auto-run)
   - TD-PAY-001 (Webhook integration test re-enable)
   - TD-PAY-002 (Signature validation fix)

   Sprint 2 (Fase 3 - Minggu 2):
   - TD-ARCH-001 (Redis connection cleanup)
   - TD-CODE-001 (Error handling any)
   - TD-CODE-002 (Slot availability refactor)
   - TD-PAY-003 (Idempotency key from header)
   - TD-PAY-005 (Handle all transaction statuses)

   Sprint 3 (Fase 3 - Minggu 3):
   - TD-DB-001 (Invoice number & payment method)
   - TD-DB-002 (Audit log FK)
   - TD-TEST-003 (Queue concurrency E2E)
   - TD-PAY-004 (Retry integration test)
   - TD-PAY-006 (Invoice lookup fix)

   Backlog (Kapan Saja):
   - TD-CODE-003 (Hardcoded branch ID)
   - TD-CODE-004 (Notification integration)
   - TD-ARCH-002 (Redis URL consistency)
   - TD-DOC-001 (API docs)
   - TD-DOC-002 (README)
   - TD-PAY-007 (Refund webhook)
   - TD-PAY-008 (Webhook rate limiting)

4. CATATAN
   - Semua technical debt di atas bersifat "aman" untuk sementara karena 
     tidak menyebabkan crash di production (kecuali audit log FK yang 
     hanya error logging, tidak break flow).
   - Prioritas perbaikan mengikuti risk assessment: 
     High = berpotensi mengganggu stabilitas sistem / test suite.
     Medium = mengurangi kualitas kode atau maintainability.
     Low = kosmetik atau dokumentasi.
   - Item dengan ID TD-PAY-007 dan TD-PAY-008 bisa di-defer ke V2 
     karena fitur refund dan rate limiting khusus webhook tidak kritis 
     untuk MVP.

   2.8. PAYMENT INPUT & RECONCILIATION (TASK 2.4.3)
   ──────────────────────────────────────────────────────────────
   ID : TD-PAY-014
   Judul : Reconciliation Endpoint Belum Diuji dengan Integration Test
   Deskripsi : `GET /payments/reconciliation` sudah diimplementasikan,
               tapi belum ada integration test yang memverifikasi endpoint
               tersebut dengan database real.
   Dampak : Risiko endpoint gagal di production karena perbedaan behavior
            antara mock dan database real.
   Perbaikan : Tambahkan integration test di `tests/integration/payment-reconciliation.test.ts`
               untuk menguji endpoint reconciliation dengan data real.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-PAY-015
   Judul : PaymentInputService Tidak Memiliki Retry Mechanism untuk Webhook
   Deskripsi : `PaymentInputService.processPaymentInput()` langsung
               mengembalikan error jika invoice tidak ditemukan, tanpa
               mekanisme retry atau queue untuk diproses ulang nanti.
   Dampak : Jika webhook datang sebelum invoice dibuat (misal karena
            invoice generation async), webhook akan gagal dan harus
            di-replay manual oleh admin.
   Perbaikan : Integrasikan dengan PaymentWebhookService.retryFailedWebhooks()
               atau tambahkan queue (RabbitMQ) untuk menunda processing
               sampai invoice tersedia.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-016
   Judul : Invoice Lookup di PaymentInputService Masih Mengandalkan invoice_number
   Deskripsi : `PaymentInputService` mencari invoice berdasarkan `invoice_number`
               atau `booking_id`. Jika kolom `invoice_number` belum ada
               (tergantung TD-DB-001), lookup akan gagal.
   Dampak : Webhook akan gagal jika invoice_number tidak tersimpan di DB.
   Perbaikan : Pastikan TD-DB-001 (invoice_number column) selesai, dan
               update PaymentInputService untuk menggunakan parsing order_id
               yang lebih robust.
   Estimasi : 2 jam (tergantung TD-DB-001)
   Prioritas : HIGH (terkait TD-DB-001)

   2.9. INVOICE GENERATION TRIGGERED BY EMR (TASK 2.4.4)
   ──────────────────────────────────────────────────────────────
   ID : TD-PAY-016
   Judul : Trigger Database Tidak Memperhitungkan Biaya Treatment
   Deskripsi : Trigger `generate_invoice_on_encounter_complete()` hanya
               memasukkan `amount_due = 0`. Biaya treatment harus dihitung
               dari service pricing yang belum diimplementasikan.
   Dampak : Invoice yang dihasilkan otomatis memiliki amount 0, admin harus
            manual update amount setelah invoice dibuat.
   Perbaikan : Tambahkan logika perhitungan biaya berdasarkan treatment
               atau service code di trigger atau service.
   Estimasi : 2-3 jam
   Prioritas : HIGH

   ID : TD-PAY-017
   Judul : Encounter Completion Endpoint Belum Diuji Integration Test
   Deskripsi : `POST /api/encounters/:id/complete` hanya di-cover unit test,
               belum ada integration test dengan database real dan trigger.
   Dampak : Risiko trigger tidak berjalan di production atau invoice tidak
            terbentuk jika ada error di trigger.
   Perbaikan : Tambahkan integration test untuk complete encounter dan
               verifikasi invoice terbuat di database.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   2.10. WEBHOOK RETRY AUTO-RECOVERY (TASK 2.4.5)
   ──────────────────────────────────────────────────────────────
   ID : TD-PAY-018
   Judul : Webhook Retry Integration Test Belum Ada
   Deskripsi : `WebhookRetryJob` dan `admin replay` hanya di-cover unit test.
               Belum ada integration test yang memverifikasi cron job benar-benar
               berjalan di environment real dengan database dan redis.
   Dampak : Risiko retry logic tidak berfungsi di production karena perbedaan
            environment (timezone, database connection, redis).
   Perbaikan : Tambahkan integration test dengan testcontainers atau mock
               yang lebih realistis menggunakan fakta waktu (sinon.useFakeTimers).
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-019
   Judul : Auto-Cancel 24h Tidak Diuji dengan Integration Test
   Deskripsi : Logika auto-cancel booking setelah 24h hanya di-cover di
               `handleAutoCancel` tanpa test yang memverifikasi trigger
               benar-benar berjalan di database.
   Dampak : Risiko booking tidak auto-cancel jika payment gagal.
   Perbaikan : Tambahkan integration test yang mensimulasikan waktu 24 jam
               menggunakan fakta waktu di database.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-PAY-020
   Judul : Admin Replay Idempotency Belum Diuji
   Deskripsi : Endpoint `POST /api/admin/webhooks/:id/replay` memiliki logika
               idempotency (cek jika sudah PROCESSED), tapi belum ada test
               yang memverifikasi perilaku ini.
   Dampak : Risiko admin replay menyebabkan double-charge jika webhook sudah
            diproses tapi error record masih ada.
   Perbaikan : Tambahkan unit test untuk memverifikasi idempotency admin replay.
   Estimasi : 1 jam
   Prioritas : MEDIUM

   ID : TD-PAY-021
   Judul : Bypass Trigger Menggunakan session_replication_role
   Deskripsi : Untuk undo payment admin override, kita menggunakan
               `SET session_replication_role = replica` untuk bypass trigger.
               Ini adalah praktik yang tidak ideal dan berbahaya.
   Dampak : Jika ada bug di trigger, admin override bisa menyebabkan data
            inkonsistensi karena trigger yang seharusnya jalan jadi di-skip.
   Perbaikan : Buat mekanisme undo yang lebih aman, misalnya dengan menambahkan
               kolom `is_manual_override` atau menggunakan soft delete + re-create
               daripada bypass trigger.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-PAY-022
   Judul : Retry Job Tidak Memiliki Monitoring/Alerting
   Deskripsi : WebhookRetryJob berjalan setiap 5 menit, tapi tidak ada
               monitoring atau alerting jika webhook permanently failed.
   Dampak : Admin tidak secara proaktif diberitahu jika payment webhook gagal
            permanen, sehingga booking bisa tertahan tanpa diketahui.
   Perbaikan : Tambahkan alerting (email/Slack) jika webhook permanently failed,
               dan dashboard monitoring untuk status retry.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   2.11. DOCTOR QUEUE & ENCOUNTER (TASK 2.5.2 - 2.5.4)
   ──────────────────────────────────────────────────────────────
   ID : TD-TEST-006
   Judul : Integration Test untuk Doctor Queue Endpoints Belum Ada
   Deskripsi : Endpoint GET /api/doctors/:id/queue, POST /api/queue/:id/check-in,
               dan GET /api/doctors/:id/queue/stats hanya di-cover oleh unit test.
               Belum ada integration test yang menguji endpoint tersebut dengan
               database real dan autentikasi.
   Dampak : Risiko endpoint gagal di production karena perbedaan behavior
            antara mock dan database real, terutama untuk validasi queue status
            dan pembuatan encounter.
   Perbaikan : Tambahkan integration test di tests/integration/doctor-queue.integration.test.ts
               yang menguji:
               - GET /doctors/:id/queue mengembalikan queue yang benar
               - POST /queue/:id/check-in berhasil update queue dan buat encounter
               - GET /doctors/:id/queue/stats mengembalikan statistik akurat
               - Error cases: queue not found, not owned, not waiting
   Estimasi : 2-3 jam
   Prioritas : HIGH

   ID : TD-CODE-005
   Judul : Duplikasi Logika Queue Management antara QueueService dan DoctorQueueService
   Deskripsi : QueueService (Task 2.3.2) dan DoctorQueueService (Task 2.5.2)
               memiliki logika yang tumpang tindih untuk queue management.
               QueueService fokus pada Redis INCR dan reset, sedangkan DoctorQueueService
               fokus pada fetch queue per doctor dan check-in patient.
               Namun keduanya mengakses tabel queues dan ada potensi inkonsistensi.
   Dampak : Perubahan aturan queue harus dilakukan di dua tempat, rawan inkonsistensi.
   Perbaikan : Refactor menjadi satu QueueManagementService yang menangani:
               - generate queue number (Redis INCR)
               - fetch queue per doctor/branch
               - update queue status (call, complete)
               - queue statistics
               - reset queue
               DoctorQueueService dapat menggunakan QueueManagementService untuk
               operasi dasar, dan menambahkan logika encounter.
   Estimasi : 2-3 jam
   Prioritas : MEDIUM

   ID : TD-TEST-007
   Judul : Integration Test untuk Patient History Endpoints Belum Ada
   Deskripsi : Endpoint GET /api/patients/:id/history dan GET /api/patients/:id/medical-records
               hanya di-cover oleh unit test. Belum ada integration test dengan database real.
   Dampak : Risiko endpoint gagal di production karena perbedaan query dengan database real.
   Perbaikan : Tambahkan integration test di tests/integration/patient-history.integration.test.ts.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-CODE-006
   Judul : EncounterService Tidak Memiliki Error Handling yang Konsisten untuk Status Encounter
   Deskripsi : Di EncounterService, validasi status encounter (ONGOING, COMPLETED, CANCELLED)
               dilakukan di beberapa method, tapi tidak ada enum atau constant yang terpusat.
   Dampak : Jika ada penambahan status baru, harus di-update di banyak tempat.
   Perbaikan : Buat enum EncounterStatus di models/encounter.model.ts dan gunakan di seluruh service.
   Estimasi : 30 menit
   Prioritas : LOW

   ID : TD-TEST-008
   Judul : Unit Test Audit Service untuk EMR Events Sudah Ada, Tapi Belum Menguji Retrieval dengan Filter
   Deskripsi : Test audit.emr.test.ts sudah menguji logEvent untuk 4 event EMR,
               tapi belum menguji getEventsByEntity dengan filter entityType dan entityId.
   Dampak : Tidak teruji apakah audit log untuk encounter tertentu bisa di-retrieve dengan benar.
   Perbaikan : Tambahkan test case yang memanggil getEventsByEntity dengan entityType='ENCOUNTER'
               dan entityId tertentu, lalu verifikasi hasilnya.
   Estimasi : 30 menit
   Prioritas : LOW

   2.12. WALK-IN TEMPORARY PROFILE (TASK 2.6.1)
   ──────────────────────────────────────────────────────────────
   ID : TD-WALKIN-001
   Judul : WalkInService.bookAppointment() Tidak Memiliki Validasi Slot Availability
   Deskripsi : Saat admin membuat booking untuk walk-in, service tidak melakukan
               pengecekan ketersediaan slot dokter (max 4 pasien per sesi) seperti
               yang dilakukan di BookingService.createScheduledBooking().
   Dampak : Walk-in bisa melebihi kapasitas maksimal per sesi (4 pasien),
            sehingga dokter kelebihan pasien.
   Perbaikan : Tambahkan validasi slot availability di bookAppointment()
               sebelum membuat booking. Gunakan helper atau repository yang sama
               dengan BookingService.
   Estimasi : 1-2 jam
   Prioritas : HIGH

   ID : TD-WALKIN-002
   Judul : Tidak Ada Rate Limiting untuk Endpoint Public /walk-in/create-profile
   Deskripsi : Endpoint POST /walk-in/create-profile bersifat public (tanpa auth)
               dan tidak memiliki rate limiting. Ini rentan terhadap spam atau DDoS.
   Dampak : Potensi serangan DDoS atau database overload karena bisa membuat
            ribuan temporary profiles dalam waktu singkat.
   Perbaikan : Tambahkan rate limiting khusus untuk endpoint public ini
               (misal 10 request per IP per menit).
   Estimasi : 30 menit
   Prioritas : MEDIUM

   ID : TD-WALKIN-003
   Judul : Temporary Patients Tidak Punya Mekanisme Cleanup Otomatis
   Deskripsi : Temporary profiles yang tidak pernah dikonversi menjadi patient
               akan menumpuk di database tanpa ada mekanisme penghapusan otomatis.
   Dampak : Database akan penuh dengan data temporary yang tidak terpakai
            dalam jangka panjang.
   Perbaikan : Tambahkan cron job yang menghapus temporary profiles yang
               sudah berumur > 7 hari dan belum dikonversi.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   2.13. NO-SHOW DETECTION (TASK 2.6.2)
   ──────────────────────────────────────────────────────────────
   ID : TD-NOSHOW-001
   Judul : NO-SHOW Detection Tidak Memiliki Integration Test
   Deskripsi : NoShowDetectionJob hanya di-cover oleh unit test (mock).
               Belum ada integration test yang memverifikasi cron job
               benar-benar berjalan di environment real dengan database.
   Dampak : Risiko cron job tidak berfungsi di production karena perbedaan
            environment atau bug yang tidak terdeteksi.
   Perbaikan : Tambahkan integration test untuk NoShowDetectionJob dengan
               database test yang diisi data booking masa lalu.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   ID : TD-NOSHOW-002
   Judul : Grace Period 15 Menit Hardcoded di NoShowDetectionJob
   Deskripsi : Interval grace period (15 menit) di-hardcode di query.
               Tidak bisa diubah tanpa mengubah kode.
   Dampak : Sulit untuk mengatur grace period sesuai kebijakan klinik
            tanpa deploy ulang.
   Perbaikan : Pindahkan grace period ke environment variable atau
               database settings (misal config table).
   Estimasi : 30 menit
   Prioritas : LOW

   ID : TD-NOSHOW-003
   Judul : Tidak Ada Notifikasi untuk NO-SHOW yang Baru Di-Mark
   Deskripsi : Saat cron job menandai booking sebagai NO_SHOW, tidak ada
               notifikasi ke admin atau pasien.
   Dampak : Admin tidak tahu ada no-show baru untuk ditindaklanjuti,
            pasien tidak mendapat pemberitahuan.
   Perbaikan : Tambahkan notifikasi (email/FCM) ke admin dan pasien saat
               booking di-mark sebagai NO_SHOW.
   Estimasi : 1-2 jam
   Prioritas : MEDIUM

   2.14. ATTENDANCE METRICS (TASK 2.6.3)
   ──────────────────────────────────────────────────────────────
   ID : TD-ATTENDANCE-001
   Judul : Query getNoShowSummary Tidak Menggunakan Index untuk Filter Branch & Status
   Deskripsi : Query untuk no-show summary menggunakan filter branch_id dan
               status NO_SHOW, tapi belum ada index yang mengcover kombinasi ini.
   Dampak : Query bisa menjadi lambat jika data bookings sudah mencapai
            puluhan ribu records.
   Perbaikan : Tambahkan index composite: idx_bookings_branch_status_no_show
               ON bookings(branch_id, status) WHERE status = 'NO_SHOW'.
   Estimasi : 30 menit
   Prioritas : MEDIUM

   ID : TD-ATTENDANCE-002
   Judul : Attendance Report Tidak Menyertakan Walk-in Patients dengan Benar
   Deskripsi : Patient attendance hanya menghitung bookings dengan pasien_id.
               Walk-in patients yang tidak memiliki user account (pasien_id = NULL)
               tidak terhitung dalam attendance.
   Dampak : Statistik kehadiran tidak akurat karena walk-in patients tidak
            masuk dalam perhitungan.
   Perbaikan : Pertimbangkan untuk mencatat attendance untuk walk-in patients
               menggunakan temp_patient_id atau buat user account otomatis
               untuk walk-in yang sering datang.
   Estimasi : 2-3 jam
   Prioritas : LOW

================================================================
END OF TECHNICAL DEBT REGISTER (v1.1 - Updated 2026-07-23)
================================================================